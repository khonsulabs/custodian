[package]
name = "custodian-password"
version = "0.0.0-dev.1"
description = "Password management simplified."
readme = "../README.md"
license = "MIT OR Apache-2.0"
repository = "https://github.com/khonsulabs/custodian"
keywords = ["password", "authentication", "login", "pake", "opaque"]
categories = ["authentication"]
edition = "2018"

[features]
default = ["sha3"]
blake3 = ["arrayvec", "blake3_"]

[dependencies]
argon2 = "0.2"
arrayvec = { version = "0.5", optional = true }
blake3_ = { package = "blake3", version = "0.3", optional = true }
curve25519-dalek = "3"
digest = "0.9"
generic-array = { version = "0.14", features = ["more_lengths"] }
opaque-ke = { package = "custodian-opaque", path = "../opaque", version = "0.6.0-pre.1", features = [
	"slow-hash"
] }
serde-big-array = { version = "0.3", features = ["const-generics"] }
sha2 = "0.9"
sha3 = { version = "0.9", optional = true }
serde = { version = "1", features = ["derive"] }
thiserror = "1"

[dev-dependencies]
anyhow = "1"
bincode = "1"
